언제 쓸까?
그냥 repository 에 있는걸로는 해결 안날 때도 있다.
복잡한 쿼리를 쓸 때나 동적 쿼리를 사용할때 필요하다.
즉, 커스텀이 필요한 쿼리가 작동될때 필요하다.
springjdbc 나 querydsl 을 섞을때도 필요하다.

또 항상 사용자 정의 리포지토리가 필요한것은 아니다.
핵심 비즈니스 로직을 위한 리포지토리와
특정한 화면에 필요한 쿼리를 분리하기 위해서도 필요하다.
때문에 인위의 리포지토리나 클래스를 만들어도 된다.

어떻게 쓸까
springconfig를 통해서 혹은 인터페이스이름+Impl 로 구현
